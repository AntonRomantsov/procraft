 {{ header }}
 <main>
  <section>
    <div class="breadcrumbs__container">
      <ul class="breadcrumbs">
        {% for breadcrumb in breadcrumbs %}
        <li class="breadcrumbs__item">
          <a class="breadcrumbs__link" href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a>
        </li>
        {% endfor %}
      </ul>
    </div>
  </section>

  <section class="section__regular category">
    <div class="category__container">
      <h2 class="category__title">{{ heading_title }}</h2>
      <div class="category__wrapper">
      {% for category in categories %}
        <a href="{{ category.href }}" class="category_item">
          <div class="category__image_wrapper">
            <img class="category__image" src="{{ category.image }}" alt="Categoty" class="category__image" />
          </div>
          <h5 class="category__name">{{ category.name }}</h5>
        </a>
      {% endfor %}
      </div>
    </div>
  </section>
  
  {% if filters %}
  <section class="section__regular subcategory">
        <div class="subcategory__container">
          <div class="subcategory__wrapper subcategory__wrapper--filter_mod">
          {% for item in filters %}
            <a href="{{ item.href }}" class="subcategory__item subcategory__item--filter_mod">
              <div class="subcategory__image_wrapper">
                <img class="subcategory__image" src="{{ item.image }}" alt="Product" />
              </div>
              <h5 class="subcategory__name">{{ item.name }}</h5>
            </a>
          {% endfor %}
          </div>
        </div>
  </section>
   {% endif %}

  <section class="section__regular subcategory_products">
  <div class="subcategory_products__container">
    <div class="subcategory_products__wrapper">
    {% for product in products %} 
      <div class="product{% if product.stock_id == 5 and product.quantity < 1 %} out-of-stock{% endif %}">
        <div class="product__wrapper">
          <div class="product__labels">
          {% if product.product_xdstickers is not empty %}
          {% for xdsticker in product.product_xdstickers %}
            <span class="product__labels-label">{{ xdsticker.text }}</span>
          {% endfor %}
          {% endif %}
          {% if product.product_xdstickers_custom is not empty %}
          {% for xdsticker in product.product_xdstickers_custom %}
            <span class="product__labels-label">{{ xdsticker.text }}</span>
          {% endfor %}
          {% endif %}
          {% if product.is_rotate %}
            <span class="product__labels-overwiev">
              <img src="catalog/view/theme/default/image//product/overview.png" alt="360" />
            </span>
          {% endif %}
          </div>
          <div class="product__like">
            <svg class="product__like-item product-like-{{ product.product_id }}{% if product.wl_class %} product__like-active{% endif %}" viewBox="0 0 24 21" fill="none" xmlns="http://www.w3.org/2000/svg" id="product-like-{{ product.product_id }}" {% if not product.wl_class %}onclick="wishlist.add({{ product.product_id }})"{% else %}onclick="wishlist.remove({{ product.product_id }})"{% endif %}>
              <path
                d="M11.9626 19.5381L2.10281 9.80024C1.07574 8.78576 0.5 7.41137 0.5 5.97966C0.5 4.548 1.07574 3.17365 2.10281 2.15928C3.13034 1.1449 4.52508 0.573975 5.98067 0.573975C7.43626 0.573975 8.83108 1.1449 9.85873 2.15928L11.9626 4.23506L14.0665 2.15928C15.094 1.1449 16.4888 0.573975 17.9444 0.573975C19.4 0.573975 20.7948 1.1449 21.8224 2.15928C22.8499 3.17365 23.4287 4.548 23.4287 5.97966C23.4287 7.41137 22.8499 8.78576 21.8224 9.80024L11.9626 19.5381Z"
              />
            </svg>
          </div>
          <div class="product__image">
            <a class="product__image-link" href="{{ product.href }}">
              <img class="product__image-item" src="{{ product.thumb }}" alt="Product" />
            </a>
          </div>
          <div class="product__content-row">
            <div class="product__content">
              <h4 class="product__title"><a href="{{ product.href }}">{{ product.name }}</a></h4>
              <div class="product__rating">
                <div class="rating">
                {% for i in 1..5 %}
                {% if product.rating < i %}
                <span class="rating-star"></span>
                {% else %}
                <span class="rating-star rating-filled"></span>
                {% endif %}
              {% endfor %}
                </div>
                <span class="product__rating-total">{{ product.reviews }}</span>
              </div>
              <div class="product__price-row">
              {% if product.special %}
              <p class="product__price">
                <span>{{ product.special }}</span>
              </p>
              {% else %}
              <p class="product__price">
                {{ product.price }}
              </p>
              {% endif %}
              {% if product.special %}
              <p class="product__price-old">{{ product.price }}</p>
              {% endif %}
              </div>
            </div>
            <div class="product__badges">
            {% if product.gifts %}
            <span class="product__badge-present">{{ text_gift }}</span>
            {% endif %}
            {% if product.is_new %}
            <span class="product__badge-new">New</span>
            {% endif %}
            {% if product.special %}
            <span class="product__badge-discount">-{{ product.percent }}%</span>
            {% endif %}
            </div>
          </div>
          <div class="product__additional">
            <button class="product__additional-button" onclick="cart.add('{{ product.product_id }}');">
              <svg class="product__additional-icon"><use href="assets/icons.svg#cartSmall"></use></svg>
              <p>{{ button_cart }}</p>
            </button>
            <ul class="product__additional-attributes">
            {% for attr in product.attributes %}
            <li class="product__additional-attribute">
              <span class="product__additional-attribute_parameter">{{ attr.name }}</span>
              <span class="product__additional-attribute_value">{{ attr.text }}</span>
            </li>
            {% endfor %}
            </ul>
          </div>
        </div>
      </div>
    {% endfor %}
   
    </div>
    <div class="row">
          <div class="col-sm-12 text-center">{{ pagination }}</div>
        </div>
  </div>
</section>

{% if video %}
<section class="section__regular subcategory_video">
        <div class="subcategory_video__container">
          <div class="subcategory_video__wrapper"></div>
          <div class="subcategory_video__block">
            <h2 class="subcategory_video__title">{{ text_video }}</h2>
            <div class="video">
              <a href="https://www.youtube.com/watch?v={{ video.id }}" class="video__wrapper">
                <img class="video__item video-item-cat" src="{{ video.thumb }}" alt="{{ video.title }}" />
                <div class="video__play-button"></div>
              </a>
              <div class="video__description">
                <p class="video__title">{{ video.title }}</p>
                <div class="video__info">
                  <span>{{ video.views }} {{ text_yt_views }}</span>
                  <span>•</span>
                  <span>{{ video.date }}</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    {% endif %}

  <section class="section__regular category_text">
    <div class="category_text__container">
      <div class="category_text__wrapper">
        {{ description }}
      </div>
    </div>
  </section>
</main>
{# <div id="product-category" class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% set class_col = 'product-layout product-grid col-lg-6 col-md-6 col-sm-12 col-xs-12' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-sm-9' %}
    {% set class_col = 'product-layout product-grid col-lg-4 col-md-4 col-sm-6 col-xs-12' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% set class_col = 'product-layout product-grid col-lg-3 col-md-3 col-sm-6 col-xs-12' %}
    {% endif %}
    <div id="content" class="{{ class }}">{{ content_top }}
      <h1>{{ heading_title }}</h1>
      {% if categories %}
      <div class="categories-page">
        <div class="row">
        {% for category in categories %}
          <div class="col-xs-6 col-lg-3">
            <div class="categories-page__item">
              <a class="categories-page__link" href="{{ category.href }}">
                <div class="categories-page__img"><img src="{{ category.image }}" alt="" class="img-responsive"></div>
                <span class="categories-page__name">{{ category.name }}</span>
              </a>
            </div>
          </div>
        {% endfor %}          
        </div>
      </div>
      {% endif %}
      <!-- OCFilter Start -->
      {% if ocfilter_description_top %}
      <div class="ocf-description ocf-description-top">{{ ocfilter_description_top }}</div>
      {% endif %}
      
      {% if ocfilter_pages.top %}
      <nav class="ocf-page-list-top">  
        {% for ocfilter_page_group in ocfilter_pages.top %}
        <div class="ocf-page-group">
          {% if ocfilter_page_group.name %}
          <div class="ocf-page-group-name">{{ ocfilter_page_group.name }}</div>
          {% endif %}
          <ul class="ocf-category-page-list">
            {% for ocfilter_page in ocfilter_page_group.pages %}           
            {% if ocfilter_page.selected %}
            <li class="ocf-page-selected"><a href="{{ ocfilter_page.href }}">{{ ocfilter_page.name }}</a></li>
            {% else %}
            <li><a href="{{ ocfilter_page.href }}">{{ ocfilter_page.name }}</a></li>
            {% endif %}            
            {% endfor %}
          </ul>         
        </div>
        {% endfor %}
      </nav>
      {% endif %} 
      
      {% if products %}
      <!-- OCFilter Start -->      
      {% if ocfilter_mobile_button %}
      <div class="ocf-btn-mobile-static ocf-mobile">
        <button type="button" class="ocf-btn ocf-btn-default" data-ocf="mobile"><span class="ocf-btn-name">{{ button_ocfilter_mobile }}</span> <i class="ocf-icon ocf-icon-16 ocf-brand ocf-sliders"></i></button>
      </div>
      {% endif %}   
      <!-- /OCFilter End -->
      <!-- /OCFilter End -->
        <div class="row category-products"> 
        {% for product in products %}      
        <div class="category-product {{ class_col }} {% if product.stock_id == 5 and product.quantity < 1 %}out-of-stock{% endif %} product-mobile-none">
          <div class="product-thumb">

					<!-- XD stickers start -->
					{% if product.product_xdstickers is not empty %}
					<div class="xdstickers_wrapper {{xdstickers_position}}">
						{% for xdsticker in product.product_xdstickers %}
							<div class="xdstickers {{xdsticker.id}}">
								{{xdsticker.text}}
							</div>
						{% endfor %}
					</div>
					{% endif %}
					{% if product.product_xdstickers_custom is not empty %}
					<div class="xdstickers_wrapper xdstickers_wrapper_custom {{xdstickers_position}}">
						{% for xdsticker in product.product_xdstickers_custom %}
							<div class="xdstickers {{xdsticker.id}}">
								{{xdsticker.text}}
							</div>
						{% endfor %}
					</div>
					{% endif %}
					<!-- XD stickers end -->
				
            <div class="image">{% if product.is_rotate %}<span class="three_de_cover"></span>{% endif %}<a href="{{ product.href }}"><img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-responsive" /></a></div>
            <div class="category-product__inner">
              <div class="caption">
                <h4><a href="{{ product.href }}">{{ product.name }}</a></h4>
                <div class="category-product__rating">
                  <div class="rating"> {% for i in 1..5 %}
                    {% if product.rating < i %} <span class="fa fa-stack"><i class="fa fa-star-o fa-stack-2x"></i></span> {% else %} <span class="fa fa-stack"><i class="fa fa-star fa-stack-2x"></i><i class="fa fa-star-o fa-stack-2x"></i></span>{% endif %}
                    {% endfor %} 
                  </div>
                  <span class="category-product__rating-total">{{ product.reviews }}</span>
                </div>
                {% if product.stock_id == 5 and product.quantity < 1 %}
                <a class="product-actions__form-btn" href="javascript:OpenFormBuilderPopup(2);">{{ text_form_stock }}</a>
                {% else %}
                {% if product.price %}
                <p class="price"> {% if not product.special %}
                  {{ product.price }}
                  {% else %} <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span>
                  <span class="tb_label_special">-{{ product.percent }}%</span> {% endif %}
                  {% if product.tax %} <span class="price-tax">{{ text_tax }} {{ product.tax }}</span> {% endif %} </p>
                {% endif %}
                {% endif %}
                </div>
                {% if product.wl_class %}
                  <button class="category-product__heart {{ product.wl_class }}" type="button" data-toggle="tooltip" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}');"><i class="fa fa-heart"></i></button>
                {% else %}
                  <button class="category-product__heart {{ product.wl_class }}" type="button" data-toggle="tooltip" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}', $(this));"><i class="fa fa-heart-o"></i></button>
                {% endif %}
            </div>
          </div>
          <div class="category-product__footer">
            {% if product.stock_id == 5 and product.quantity < 1 %}
            {% else %}
            <button class="btn btn-primary category-product__btn" type="button" onclick="cart.add('{{ product.product_id }}', '{{ product.minimum }}');"><i class="fa fa-shopping-cart hidden-lg"></i> <span class="hidden-xs hidden-sm hidden-md">{{ button_cart }}</span></button>
            {% endif %}
            {% if product.attributes %}  
            <div class="category-product__attributes">
              {% for attribute_group in product['attributes'] %} 
              {% for i, attribute in attribute_group['attribute'] %} 
              {% if (i < 4) %} 
                <div class="category-product__attribute"><span>{{ attribute['name'] }}</span>  {{ attribute['text'] }} </div>
              {% endif %} 
              {% endfor %}
              {% endfor %}
            </div>
            {% endif %}
          </div>
        </div>
        {% endfor %} 
        {% for product in products %} 
        <div class="category-product {{ class_col }} {% if product.stock_id == 5 and product.quantity < 1 %}out-of-stock{% endif %} product-desctop-none">
          <div class="product-thumb">
            <div class="mobile-left-side">

					<!-- XD stickers start -->
					{% if product.product_xdstickers is not empty %}
					<div class="xdstickers_wrapper {{xdstickers_position}}">
						{% for xdsticker in product.product_xdstickers %}
							<div class="xdstickers {{xdsticker.id}}">
								{{xdsticker.text}}
							</div>
						{% endfor %}
					</div>
					{% endif %}
					{% if product.product_xdstickers_custom is not empty %}
					<div class="xdstickers_wrapper xdstickers_wrapper_custom {{xdstickers_position}}">
						{% for xdsticker in product.product_xdstickers_custom %}
							<div class="xdstickers {{xdsticker.id}}">
								{{xdsticker.text}}
							</div>
						{% endfor %}
					</div>
					{% endif %}
					<!-- XD stickers end -->
				
            <div class="image">{% if product.is_rotate %}<span class="three_de_cover"></span>{% endif %}<a href="{{ product.href }}"><img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-responsive" /></a></div>
            <div class="category-product__rating">
              <div class="rating"> {% for i in 1..5 %}
                    {% if product.rating < i %} <span class="fa fa-stack"><i class="fa fa-star-o fa-stack-2x"></i></span> {% else %} <span class="fa fa-stack"><i class="fa fa-star fa-stack-2x"></i><i class="fa fa-star-o fa-stack-2x"></i></span>{% endif %}
                    {% endfor %} 
                    <span class="category-product__rating-total">{{ product.reviews }}</span>
                  </div>
            </div>
                </div>
            <div class="category-product__inner">
              <div class="caption">
                <h4><a href="{{ product.href }}">{{ product.name }}</a></h4>
                
                {% if product.attributes %}  
            <div class="category-product__attributes">
              {% for attribute_group in product['attributes'] %} 
              {% for i, attribute in attribute_group['attribute'] %} 
              {% if (i < 4) %} 
                <div class="category-product__attribute"><span>{{ attribute['name'] }}</span>  {{ attribute['text'] }} </div>
              {% endif %} 
              {% endfor %}
              {% endfor %}
            </div>
            {% endif %}
            <div class="price-wrap">
                {% if product.stock_id == 5 and product.quantity < 1 %}
                <a class="product-actions__form-btn" href="javascript:OpenFormBuilderPopup(2);">{{ text_form_stock }}</a>
                {% else %}
                {% if product.price %}
                <p class="price"> {% if not product.special %}
                  {{ product.price }}
                  {% else %} <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span>
                  <span class="tb_label_special">-{{ product.percent }}%</span> {% endif %}
                  {% if product.tax %} <span class="price-tax">{{ text_tax }} {{ product.tax }}</span> {% endif %} </p>
                {% endif %}
                {% endif %}
                {% if product.stock_id == 5 and product.quantity < 1 %}
                {% else %}
                <button class="btn btn-primary category-product__btn" type="button" onclick="cart.add('{{ product.product_id }}', '{{ product.minimum }}');"><i class="fa fa-shopping-cart hidden-lg"></i> <span class="hidden-xs hidden-sm hidden-md">{{ button_cart }}</span></button>
                {% endif %}
              </div>
                </div>
                {% if product.wl_class %}
                  <button class="category-product__heart {{ product.wl_class }}" type="button" data-toggle="tooltip" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}');"><i class="fa fa-heart"></i></button>
                {% else %}
                  <button class="category-product__heart {{ product.wl_class }}" type="button" data-toggle="tooltip" title="{{ button_wishlist }}" onclick="wishlist.add('{{ product.product_id }}', $(this));"><i class="fa fa-heart-o"></i></button>
                {% endif %}
            </div>
            
          </div>
          <div class="category-product__footer">
            {% if product.stock_id == 5 and product.quantity < 1 %}
            {% else %}
            <!-- <button class="btn btn-primary category-product__btn" type="button" onclick="cart.add('{{ product.product_id }}', '{{ product.minimum }}');"><i class="fa fa-shopping-cart hidden-lg"></i> <span class="hidden-xs hidden-sm hidden-md">{{ button_cart }}</span></button> -->
            {% endif %}
            
          </div>
        </div>
        {% endfor %} 
        </div>
        <div class="row">
          <div class="col-sm-12 text-center">{{ pagination }}</div>
        </div>
      {% endif %}
      {% if description %}
      <div class="row">
        <div class="col-sm-12">{{ description }}</div>
      </div>
      {% endif %}
      {% if not categories and not products %}
      <p>{{ text_empty }}</p>
      <div class="buttons">
        <div class="pull-right"><a href="{{ continue }}" class="btn btn-primary">{{ button_continue }}</a></div>
      </div>
      {% endif %}
      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div> #}
{{ footer }} 
